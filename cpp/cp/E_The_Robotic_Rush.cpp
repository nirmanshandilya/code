#include <bits/stdc++.h>
using namespace std;
int main()
{
    int tc;
    cin >> tc;
    while (tc--)
    {
        long n, m, k;
        cin >> n >> m >> k;
        long a[n];
        long b[m];
        // location of robots
        for (int i = 0; i < n; i++)
        {
            cin >> a[i];
        }
        // location of spikes
        for (int i = 0; i < m; i++)
        {
            cin >> b[i];
        }
        // intstructions
        string s;
        cin >> s;
    }
}